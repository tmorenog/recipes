Connect Recipe Scout’s backend to the coordinator using MCP. The app itself talks to the coordinator while it runs. The MCP server is at this address: {{SITE}}/api/mcp?agent=scout

For now, only establish the connection and discover the tools offered by the coordinator. Do not give the tools to the Scout Agent yet, and do not change how it works.

The coordinator requires two things on every request: our class key and our group name. All requests to the coordinator must be made from the backend.

Create a protected Lovable secret called CLASS_KEY and ask me to enter its value. Never place the class key in the page, browser code, logs or AI instructions. Our group name is {{GROUP}}.

Once you have established the connection, confirm the connection and explain the tools that are available. Show the connection status, including the group name the coordinator confirms, and the tools, with their descriptions, in a “Coordinator connection” section on the page.
