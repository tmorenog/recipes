Now let Recipe Scout use the coordinator tools discovered in the previous step.

At the beginning of every run, ask the MCP server which tools are currently available. Give those tools to the Scout exactly as the coordinator describes them. Preserve their names, descriptions and input requirements.

Do not write the coordinator’s tool names, recipe format, validation rules or limits into the app.

Replace the Scout’s local recipe-selection action from Step 2 with the appropriate coordinator tool. Keep the actions that search TheMealDB and the action that ends the run.

These are the goals and rules for the Scout:

- Begin each run by using the coordinator tool that explains how to work with the coordinator.
- Follow the formats, rules and limits returned by the coordinator.
- Use TheMealDB to find and examine real recipes.
- Save selected recipes through the coordinator so the next agent can use them.
- If the coordinator rejects a recipe, read all its feedback, correct the recipe and try again.
- Treat a rejection as useful feedback, not as an application failure.
- Never invent recipe information.
- Finish after five recipes have been accepted.
- If five recipes cannot be accepted, explain why.

Run every tool requested by the Scout and return the complete result to it.

If the coordinator returns a tool result marked as an error, pass the complete result back to the Scout so it can correct its work. Do not treat a rejected recipe as a crashed run.

Update the page:

- Rename the recipe results area “Saved recipes”
- Show coordinator requests and responses in “What the Scout is doing”
- Make acceptances and rejections easy to distinguish
- Highlight rejection feedback without presenting it as a technical failure
- Show how many actions the Scout has used
- Add a link to the class Coordinator page: {{SITE}}/coordinator
- Keep a history of completed runs that can be reopened

The Coordinator connection section from Step 3a can remain available as a collapsible technical-details section.
