Now let the Scout Agent save its recipes to the coordinator, using the tools you discovered when connecting.

At the start of every run, the app asks the coordinator which tools it offers, and gives them to the Scout Agent as the coordinator describes them, with the same names, descriptions and inputs. Let the coordinator describe its own formats, rules and limits, every time the agent is run.

Instead of adding recipes to its own list, the Scout Agent now saves them to the coordinator. Keep the actions that search TheMealDB and the action that ends the run.

Goals for the Scout Agent:

- First use the coordinator tool that explains how to work with the coordinator, and follow what it says.
- Find real recipes on TheMealDB that fit the theme, and save them to the coordinator.
- If the coordinator rejects a recipe, read its feedback, correct the recipe and try again.
- Never invent recipe information. Finish once four recipes have been accepted; if that isn’t possible, explain why.

Run every tool the Scout Agent asks for and return the complete result to it, including rejections: a rejection is feedback, not a crash. Keep the app’s existing checks (a recipe’s complete details must be retrieved during this run before it’s sent; stop once four are accepted), and raise the limit to 35 actions.

Update the page:

- Rename the results area “Saved recipes” and show only the recipes the coordinator accepted
- In “What the Scout Agent is doing”, show the coordinator’s requests and answers, and make acceptances and rejections easy to tell apart, without showing rejections as crashes
- A link to the class Coordinator page ({{SITE}}/coordinator)
- Keep the “Coordinator connection” section, collapsed
