Enable Lovable Cloud. Connect Meal Planner’s backend to the class coordinator using MCP. The MCP server is at this address: {{SITE}}/api/mcp?agent=planner

For this step, only establish the connection, discover the tools offered by the coordinator and use them to show the class’s recipes. Do not add an AI agent yet.

The coordinator requires two things on every request: our class key, sent as the header “Authorization: Bearer ” followed by CLASS_KEY, and our group name, {{GROUP}}, sent as the header “X-Group”. If the group name is YOUR-GROUP-NAME, ask me for our group name first.

Create a protected Lovable secret called CLASS_KEY and ask me to enter its value. Never place the class key in the page, browser code, logs or AI instructions. All requests to the coordinator must be made from the backend.

Once you have established the connection:

- Explain the tools that are available and the information each one requires.
- Use the coordinator tool that explains how to work with the coordinator, and show its answer in a collapsible “How the coordinator works” section.
- Get the recipes the class has saved that already have a price, and show them in an “Available recipes” table below the week: photo, name, cuisine, cost per serving and how many groups picked it.

Each coordinator tool returns its answer as text that contains JSON. Parse it once, in the backend, and send the page ready-to-use data; the page should not parse it again. If a section can’t be shown, display a short message in that section instead of breaking the page.

If there are no priced recipes yet, say so plainly instead of showing an error.
