Our coordinator is an MCP server (Streamable HTTP) with every tool the agent needs to read recipes, look up Kroger prices and save the plan: {{SITE}}/api/mcp?agent=planner

From backend code only, send the headers "Authorization: Bearer " + COORDINATOR_KEY and "X-Group: {{GROUP}}" on every request. Load its tools with tools/list and give them to the agent exactly as the server describes them: don't write the plan format or rules into our code. A result with "isError": true is a rejection to pass back to the agent, not a crash. During a run, reuse earlier search_kroger_products answers for the same term and store instead of asking again.

No AI yet. Test it: list the coordinator's tools, call get_expectations, list_recipes, find_kroger_stores for ZIP 45202, and search_kroger_products for "yellow onions" at the first store. Show me the results. Don't call save_meal_plan or mark_processed yet.
