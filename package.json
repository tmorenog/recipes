{
  "name": "recipes-api",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "vercel-build": "node scripts/setup-db.js",
    "db:setup": "node --env-file-if-exists=.env.local scripts/setup-db.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "pg": "^8.23.0",
    "zod": "^4.6.5"
  }
}
